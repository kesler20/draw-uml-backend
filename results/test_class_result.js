// testing with correct input
test('this.layout is a private internal method used to update properties of the layout object, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(updateAxis(...correct_input)).toBe(correct_output[0]);
    expect(typeof updateAxis(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout is a private internal method used to update properties of the layout object, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(updateAxis(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof updateAxis(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout is a private internal method used to update properties of the layout object, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(updateAxis(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof updateAxis(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout is a private internal method used to update properties of the layout object, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(updateAxis(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof updateAxis(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout will change font o the plot, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(styleFont(...correct_input)).toBe(correct_output[0]);
    expect(typeof styleFont(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout will change font o the plot, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(styleFont(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof styleFont(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout will change font o the plot, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(styleFont(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof styleFont(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout will change font o the plot, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(styleFont(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof styleFont(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout will change the background color of the paper and the plot, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(styleBgColor(...correct_input)).toBe(correct_output[0]);
    expect(typeof styleBgColor(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout will change the background color of the paper and the plot, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(styleBgColor(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof styleBgColor(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout will change the background color of the paper and the plot, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(styleBgColor(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof styleBgColor(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout will change the background color of the paper and the plot, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(styleBgColor(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof styleBgColor(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout can be used to add a grid to the desired axis and add color, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(addGrid(...correct_input)).toBe(correct_output[0]);
    expect(typeof addGrid(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout can be used to add a grid to the desired axis and add color, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(addGrid(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof addGrid(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout can be used to add a grid to the desired axis and add color, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(addGrid(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof addGrid(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout can be used to add a grid to the desired axis and add color, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(addGrid(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof addGrid(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout can be used to style the x or the y axis, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(styleAxis(...correct_input)).toBe(correct_output[0]);
    expect(typeof styleAxis(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout can be used to style the x or the y axis, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(styleAxis(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof styleAxis(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout can be used to style the x or the y axis, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(styleAxis(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof styleAxis(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout can be used to style the x or the y axis, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(styleAxis(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof styleAxis(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout can be used to style the text of the axis, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(styleAxisFont(...correct_input)).toBe(correct_output[0]);
    expect(typeof styleAxisFont(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout can be used to style the text of the axis, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(styleAxisFont(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof styleAxisFont(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout can be used to style the text of the axis, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(styleAxisFont(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof styleAxisFont(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout can be used to style the text of the axis, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(styleAxisFont(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof styleAxisFont(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout will add the label to the desired axis, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(addAxis(...correct_input)).toBe(correct_output[0]);
    expect(typeof addAxis(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout will add the label to the desired axis, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(addAxis(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof addAxis(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout will add the label to the desired axis, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(addAxis(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof addAxis(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout will add the label to the desired axis, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(addAxis(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof addAxis(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout can be used to change the axis to a log scale, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(addLogScale(...correct_input)).toBe(correct_output[0]);
    expect(typeof addLogScale(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout can be used to change the axis to a log scale, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(addLogScale(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof addLogScale(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout can be used to change the axis to a log scale, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(addLogScale(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof addLogScale(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout can be used to change the axis to a log scale, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(addLogScale(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof addLogScale(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('add the following object:, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(add3DStyles(...correct_input)).toBe(correct_output[0]);
    expect(typeof add3DStyles(...correct_input)).toBe(number)
});

// testing edge cases
test('add the following object:, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(add3DStyles(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof add3DStyles(...edge_case_input)).toBe(number)
});

// testing invalid types
test('add the following object:, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(add3DStyles(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof add3DStyles(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('add the following object:, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(add3DStyles(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof add3DStyles(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout will add a margin object to he body of the layout object, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(addMargin(...correct_input)).toBe(correct_output[0]);
    expect(typeof addMargin(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout will add a margin object to he body of the layout object, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(addMargin(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof addMargin(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout will add a margin object to he body of the layout object, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(addMargin(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof addMargin(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout will add a margin object to he body of the layout object, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(addMargin(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof addMargin(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout removes the grid in a particular direction by adding ``showgrid : false``, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(removeGrid(...correct_input)).toBe(correct_output[0]);
    expect(typeof removeGrid(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout removes the grid in a particular direction by adding ``showgrid : false``, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(removeGrid(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof removeGrid(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout removes the grid in a particular direction by adding ``showgrid : false``, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(removeGrid(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof removeGrid(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout removes the grid in a particular direction by adding ``showgrid : false``, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(removeGrid(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof removeGrid(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('add ``zeroline : false`` to the selected axis object, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(removeZeroLine(...correct_input)).toBe(correct_output[0]);
    expect(typeof removeZeroLine(...correct_input)).toBe(number)
});

// testing edge cases
test('add ``zeroline : false`` to the selected axis object, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(removeZeroLine(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof removeZeroLine(...edge_case_input)).toBe(number)
});

// testing invalid types
test('add ``zeroline : false`` to the selected axis object, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(removeZeroLine(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof removeZeroLine(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('add ``zeroline : false`` to the selected axis object, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(removeZeroLine(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof removeZeroLine(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout adds a legend by adding the following object to the body, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(addLegend(...correct_input)).toBe(correct_output[0]);
    expect(typeof addLegend(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout adds a legend by adding the following object to the body, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(addLegend(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof addLegend(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout adds a legend by adding the following object to the body, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(addLegend(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof addLegend(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout adds a legend by adding the following object to the body, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(addLegend(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof addLegend(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout can be used to change the configuration of the bar charts, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(styleBarChart(...correct_input)).toBe(correct_output[0]);
    expect(typeof styleBarChart(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout can be used to change the configuration of the bar charts, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(styleBarChart(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof styleBarChart(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout can be used to change the configuration of the bar charts, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(styleBarChart(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof styleBarChart(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout can be used to change the configuration of the bar charts, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(styleBarChart(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof styleBarChart(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout can be used to change the configuration of histograms, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(styleHistograms(...correct_input)).toBe(correct_output[0]);
    expect(typeof styleHistograms(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout can be used to change the configuration of histograms, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(styleHistograms(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof styleHistograms(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout can be used to change the configuration of histograms, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(styleHistograms(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof styleHistograms(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout can be used to change the configuration of histograms, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(styleHistograms(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof styleHistograms(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout can be used to group box plots, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(styleBoxPlot(...correct_input)).toBe(correct_output[0]);
    expect(typeof styleBoxPlot(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout can be used to group box plots, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(styleBoxPlot(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof styleBoxPlot(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout can be used to group box plots, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(styleBoxPlot(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof styleBoxPlot(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout can be used to group box plots, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(styleBoxPlot(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof styleBoxPlot(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout can be used when an existing layout has to be extended, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(addLayoutData(...correct_input)).toBe(correct_output[0]);
    expect(typeof addLayoutData(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout can be used when an existing layout has to be extended, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(addLayoutData(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof addLayoutData(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout can be used when an existing layout has to be extended, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(addLayoutData(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof addLayoutData(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout can be used when an existing layout has to be extended, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(addLayoutData(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof addLayoutData(...invalid_values_input)).toBe(number)
});

// testing with correct input
test('this.layout is used to return the find body of the layout, testing with correct input', () => {
    // array of arguments which are expected by the method being tested
    const correct_input = [];
    // array containing the expected correct result of the function call
    const correct_output = [];

    expect(buildLayout(...correct_input)).toBe(correct_output[0]);
    expect(typeof buildLayout(...correct_input)).toBe(number)
});

// testing edge cases
test('this.layout is used to return the find body of the layout, testing edge cases', () => {
    // array of arguments representing
    // a potential edge case where the method might be used
    const edge_case_input = [];
    // array containing the expected result of the function call
    const edge_case_output = [];

    expect(buildLayout(...edge_case_input)).toBe(edge_case_output[0]);
    expect(typeof buildLayout(...edge_case_input)).toBe(number)
});

// testing invalid types
test('this.layout is used to return the find body of the layout, testing invalid types', () => {
    // array of arguments containing an invalid type 
    const invalid_types_input = [];
    // array containing the result of the function call
    const invalid_types_output = [];

    expect(buildLayout(...invalid_types_input)).toBe(invalid_types_output[0]);
    expect(typeof buildLayout(...invalid_types_input)).toBe(number)
});

// testing invalid inputs
test('this.layout is used to return the find body of the layout, testing invalid inputs', () => {
    // array of arguments containing an invalid value 
    const invalid_values_input = [];
    // array containing the result of the function call
    const invalid_values_output = [];
    
    expect(buildLayout(...invalid_values_input)).toBe(invalid_values_output[0]);
    expect(typeof buildLayout(...invalid_values_input)).toBe(number)
});
